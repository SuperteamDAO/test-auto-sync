name: Sync Fork

on:
  push:

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.6.3
        with:
          owner: SuperteamDAO
          base: main
          head: main
